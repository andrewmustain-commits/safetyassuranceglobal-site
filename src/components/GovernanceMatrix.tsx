"use client";
import { useState } from "react";
import { productionGates } from "@/data/courses";
export default function GovernanceMatrix(){const [active,setActive]=useState("G2");const gate=productionGates.find(g=>g.id===active)!;return <div className="panel p-5 sm:p-7"><div className="flex flex-wrap gap-2">{productionGates.map(g=><button key={g.id} onClick={()=>setActive(g.id)} className={`rounded-lg border px-3 py-2 text-xs font-bold ${active===g.id?"border-sag-gold bg-sag-gold/10 text-sag-gold":"border-slate-700 text-slate-300 hover:border-slate-500"}`}>{g.id}</button>)}</div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_.8fr]"><div><p className="eyebrow">{gate.id} production gate</p><h3 className="mt-3 text-2xl font-semibold">{gate.title}</h3><p className="mt-3 text-sag-muted">{gate.purpose}</p><p className="mt-5 rounded-lg border border-sag-gold/20 bg-sag-gold/5 p-4 text-sm text-slate-200">{gate.releaseEffect}</p></div><dl className="grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-slate-900 p-4"><dt className="text-sag-muted">Authority</dt><dd className="mt-1 font-semibold">{gate.authority}</dd></div><div className="rounded-xl bg-slate-900 p-4"><dt className="text-sag-muted">AI acceleration</dt><dd className="mt-1 font-semibold">{gate.aiAllowed?"Permitted":"Not permitted"}</dd></div><div className="rounded-xl bg-slate-900 p-4"><dt className="text-sag-muted">Human review</dt><dd className="mt-1 font-semibold">{gate.humanRequired?"Mandatory":"Not mandatory"}</dd></div><div className="rounded-xl bg-slate-900 p-4"><dt className="text-sag-muted">Independent verification</dt><dd className="mt-1 font-semibold">{gate.independentVerification?"Required":"Not at this gate"}</dd></div></dl></div></div>}
