<section class="section-shell" aria-labelledby="academy-title">
  <div class="academy-shell">
    <div>
      <p class="section-label">SAG Academy</p>
      <h2 id="academy-title">Structured capability pathways from literacy to leadership.</h2>
      <p class="summary">
        The academy model supports role-based growth for executives, practitioners, and operational teams working in high-risk systems.
      </p>
      <a class="button-secondary" href="/academy">Explore SAG Academy</a>
    </div>
    <div class="track-list" role="list" aria-label="Academy tracks">
      <p role="listitem">Foundation track for assurance fluency and shared language.</p>
      <p role="listitem">Practitioner track for applied assurance methods and review quality.</p>
      <p role="listitem">Leadership track for enterprise oversight and operating model stewardship.</p>
    </div>
  </div>
</section>

<style>
  .academy-shell {
    display: grid;
    grid-template-columns: 1.25fr 1fr;
    gap: 1rem;
    border-radius: 1rem;
    border: 1px solid rgba(88, 151, 230, 0.3);
    background: linear-gradient(170deg, rgba(8, 20, 39, 0.95), rgba(5, 14, 28, 0.98));
    padding: clamp(1.15rem, 2.4vw, 1.8rem);
  }

  .section-label {
    margin: 0;
    color: var(--home-accent-green);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 0.76rem;
  }

  .academy-shell h2 {
    margin: 0.55rem 0 0;
    font-size: clamp(1.4rem, 2.3vw, 1.85rem);
    max-width: 26ch;
  }

  .summary {
    margin: 0.8rem 0 1rem;
    color: var(--home-text-muted);
    max-width: 60ch;
  }

  .track-list {
    display: grid;
    gap: 0.55rem;
  }

  .track-list p {
    margin: 0;
    border: 1px solid rgba(111, 220, 190, 0.22);
    background: rgba(111, 220, 190, 0.07);
    color: var(--home-text-muted);
    border-radius: 0.7rem;
    padding: 0.6rem 0.7rem;
    font-size: 0.94rem;
  }

  @media (max-width: 980px) {
    .academy-shell {
      grid-template-columns: 1fr;
    }
  }
</style>