"use client";
import { useMemo, useState } from "react";
import { academies } from "@/data/academies";
import { courses } from "@/data/courses";
import type { CourseDomain } from "@/types";

const domains: CourseDomain[]=["SAF","RSK","RCS","QMS","OHI","EPC","ESS","DST","LEP","MPI"];
export default function CourseSearch(){const [q,setQ]=useState("");const [domain,setDomain]=useState("");const [academy,setAcademy]=useState("");const [duration,setDuration]=useState("");const [jurisdiction,setJurisdiction]=useState("");const rows=useMemo(()=>courses.filter(c=>{const text=`${c.id} ${c.title} ${c.summary}`.toLowerCase();return (!q||text.includes(q.toLowerCase()))&&(!domain||c.domain===domain)&&(!academy||c.academy===academy)&&(!duration||(duration==="short"?c.durationHours<=3:duration==="medium"?c.durationHours>3&&c.durationHours<=5:c.durationHours>5))&&(!jurisdiction||c.jurisdiction.some(j=>j.toLowerCase().includes(jurisdiction.toLowerCase())))}),[q,domain,academy,duration,jurisdiction]);return <div><div className="panel grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-5"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search ID, title or topic" className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm outline-none focus:border-sag-glow"/><select value={domain} onChange={e=>setDomain(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm"><option value="">All domains</option>{domains.map(d=><option key={d}>{d}</option>)}</select><select value={academy} onChange={e=>setAcademy(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm"><option value="">All academies</option>{academies.map(a=><option key={a.code} value={a.code}>{a.shortName}</option>)}</select><select value={duration} onChange={e=>setDuration(e.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm"><option value="">Any duration</option><option value="short">≤ 3 hours</option><option value="medium">4–5 hours</option><option value="long">6+ hours</option></select><input value={jurisdiction} onChange={e=>setJurisdiction(e.target.value)} placeholder="Jurisdiction" className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm"/></div><p className="mt-4 text-sm text-sag-muted">{rows.length} course record{rows.length===1?"":"s"}. Status reflects the governed registry state shown here; it is not a regulator approval indicator.</p><div className="mt-5 grid gap-4 lg:grid-cols-2">{rows.map(c=><article key={c.id} className="panel p-5"><div className="flex items-center justify-between gap-3"><span className="text-xs font-bold text-sag-gold">{c.id} · v{c.version}</span><span className="rounded-full border border-slate-700 px-2.5 py-1 text-xs text-slate-300">{c.status}</span></div><h3 className="mt-4 text-lg font-semibold">{c.title}</h3><p className="mt-2 text-sm leading-6 text-sag-muted">{c.summary}</p><div className="mt-4 flex flex-wrap gap-2 text-xs text-slate-300"><span>{c.domain}</span><span>•</span><span>{c.durationHours} hrs</span><span>•</span><span>{c.jurisdiction.join(" / ")}</span><span>•</span><span>Pass {c.scoreThreshold}%</span></div><p className="mt-4 border-t border-slate-800 pt-4 text-xs leading-5 text-sag-muted">{c.claimBoundary}</p></article>)}</div></div>}
